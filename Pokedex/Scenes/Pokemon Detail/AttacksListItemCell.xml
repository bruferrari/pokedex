<UICollectionViewCell>
    <UILabel
    text="{attack.name}"
    textColor="{rgb (145,145,145)}"
    font="{theme.font.body} 16"/>

    <UILabel
    text="{attack.damage}"
    textColor="{rgb (208,2,27)}"
    font="{theme.font.body}"
    right="parent.right - 10"/>
</UICollectionViewCell>
